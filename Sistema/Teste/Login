package com.example.tests;

import java.util.regex.Pattern;
import java.util.concurrent.TimeUnit;
import org.junit.*;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.*;
import org.openqa.selenium.*;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.support.ui.Select;

public class Login {
  private WebDriver driver;
  private String baseUrl;
  private boolean acceptNextAlert = true;
  private StringBuffer verificationErrors = new StringBuffer();

  @Before
  public void setUp() throws Exception {
    driver = new FirefoxDriver();
    baseUrl = "http://localhost:8084/";
    driver.manage().timeouts().implicitlyWait(30, TimeUnit.SECONDS);
  }

  @Test
  public void testLogin() throws Exception {
    driver.get(baseUrl + "/Licita/");
    driver.findElement(By.id("j_idt9:campo_login")).clear();
    driver.findElement(By.id("j_idt9:campo_login")).sendKeys("RICARDO");
    driver.findElement(By.id("j_idt9:campo_senha")).clear();
    driver.findElement(By.id("j_idt9:campo_senha")).sendKeys("ricardo");
    driver.findElement(By.id("j_idt9:j_idt16")).click();
    driver.findElement(By.id("j_idt9:campo_senha")).clear();
    driver.findElement(By.id("j_idt9:campo_senha")).sendKeys("");
    driver.findElement(By.id("j_idt9:campo_login")).clear();
    driver.findElement(By.id("j_idt9:campo_login")).sendKeys("");
    driver.findElement(By.id("j_idt9:j_idt16")).click();
    try {
      assertEquals("Por favor insira o Login", driver.findElement(By.cssSelector("div.ui-growl-item")).getText());
    } catch (Error e) {
      verificationErrors.append(e.toString());
    }
    driver.findElement(By.id("j_idt9:j_idt16")).click();
    try {
      assertEquals("Por favor insira a Senha", driver.findElement(By.xpath("//div[@id='j_idt9:growlogin_container']/div[2]/div/div[2]/span")).getText());
    } catch (Error e) {
      verificationErrors.append(e.toString());
    }
    driver.findElement(By.id("j_idt9:campo_login")).clear();
    driver.findElement(By.id("j_idt9:campo_login")).sendKeys("ricardo");
    driver.findElement(By.id("j_idt9:j_idt16")).click();
    try {
      assertEquals("Por favor insira a Senha", driver.findElement(By.cssSelector("span.ui-growl-title")).getText());
    } catch (Error e) {
      verificationErrors.append(e.toString());
    }
    driver.findElement(By.id("j_idt9:campo_login")).click();
    driver.findElement(By.id("j_idt9:campo_login")).clear();
    driver.findElement(By.id("j_idt9:campo_login")).sendKeys("");
    driver.findElement(By.id("j_idt9:campo_senha")).clear();
    driver.findElement(By.id("j_idt9:campo_senha")).sendKeys("ricardo");
    driver.findElement(By.id("j_idt9:j_idt16")).click();
    try {
      assertEquals("Por favor insira o Login", driver.findElement(By.cssSelector("span.ui-growl-title")).getText());
    } catch (Error e) {
      verificationErrors.append(e.toString());
    }
    driver.findElement(By.id("j_idt9:campo_senha")).clear();
    driver.findElement(By.id("j_idt9:campo_senha")).sendKeys("");
    driver.findElement(By.id("j_idt9:campo_login")).clear();
    driver.findElement(By.id("j_idt9:campo_login")).sendKeys("admin");
    driver.findElement(By.id("j_idt9:campo_senha")).clear();
    driver.findElement(By.id("j_idt9:campo_senha")).sendKeys("admin");
    driver.findElement(By.id("j_idt9:j_idt16")).click();
    try {
      assertEquals("", driver.findElement(By.id("bt_pregoes")).getText());
    } catch (Error e) {
      verificationErrors.append(e.toString());
    }
    driver.findElement(By.id("j_idt13:bot_sair")).click();
    try {
      assertEquals("Entrar", driver.findElement(By.id("j_idt9:j_idt16")).getText());
    } catch (Error e) {
      verificationErrors.append(e.toString());
    }
  }

  @After
  public void tearDown() throws Exception {
    driver.quit();
    String verificationErrorString = verificationErrors.toString();
    if (!"".equals(verificationErrorString)) {
      fail(verificationErrorString);
    }
  }

  private boolean isElementPresent(By by) {
    try {
      driver.findElement(by);
      return true;
    } catch (NoSuchElementException e) {
      return false;
    }
  }

  private boolean isAlertPresent() {
    try {
      driver.switchTo().alert();
      return true;
    } catch (NoAlertPresentException e) {
      return false;
    }
  }

  private String closeAlertAndGetItsText() {
    try {
      Alert alert = driver.switchTo().alert();
      String alertText = alert.getText();
      if (acceptNextAlert) {
        alert.accept();
      } else {
        alert.dismiss();
      }
      return alertText;
    } finally {
      acceptNextAlert = true;
    }
  }
}
